name: python_linter

on:
  workflow_dispatch:

jobs:
  python_linter:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Python Linter
      uses: sunnysid3up/python-linter@master
      with:
        strict: "high"
